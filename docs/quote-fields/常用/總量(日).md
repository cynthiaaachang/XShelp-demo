# 總量(日) – （報價欄位） <kbd>常用</kbd> <kbd>量能</kbd>

| 項目 | 說明 |
| ---- | ---- |
| 欄位名稱 | 總量(日) |
| 欄位分類 | 常用、量能 |
| 代碼 | `DailyVolume`<br>`q_DailyVolume` |
| 單位 | 台股:張；大盤/指數:元；期貨/選擇權:口；其他市場股票:股 |
| 格式 | 數值 |
| 支援腳本 | 警示、交易、函數 |
| 支援商品 | 全部 |

## 語法
> 當日的總成交量。也可以使用 GetField("Volume", "D") 來表示。  
> 數值單位依照商品類型而異。  

```pascal
Value1 = GetQuote("總量(日)");
Value1 = GetQuote("DailyVolume");
Value1 = q_DailyVolume
```

---

## 說明
當日的總成交量。也可以使用 GetField("Volume", "D") 來表示。
數值單位依照商品類型而異。
