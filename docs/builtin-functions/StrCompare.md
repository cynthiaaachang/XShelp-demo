## StrCompare（字串比較）

**回傳類型**: 1\
**參數數量**: 3

### 說明

比較字串是否相同 數值 = StrCompare(字串1, 字串2) 數值 =
StrCompare(字串1, 字串2, 不區分大小寫) 傳入二個以上參數: -
第一個參數是字串1。 - 第二個參數是字串2。 -
第三個參數是選用參數，True表示不區分大小寫，False代表區分大小寫，預設為不區分大小寫。

### 參數

-   \*\*\*\*（type: 3）
-   \*\*\*\*（type: 3）
-   \*\*\*\*（type: 2）