## GetFieldPublishDate（資料更新日期）

**回傳類型**: 1\
**參數數量**: 2

### 說明

用來取得指定欄位在XQ中更新的日期。

GetFieldPublishDate(欄位名稱, 頻率); 第一個參數是要查詢的欄位名稱。
第二個參數是要查詢的欄位頻率，預設值為執行頻率。
回傳值為YYYYMMDD的8碼數字。

### 參數

-   **欄位名稱**（type: 3）
-   **頻率**（type: 3）