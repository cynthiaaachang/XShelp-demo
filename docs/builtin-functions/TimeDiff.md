## TimeDiff（計算兩個時間之間的差異）

**回傳類型**: 1\
**參數數量**: 3

### 說明

計算兩個時間數值的差異 差異 = TimeDiff(第一個時間數值，第二個時間數值,
要計算差異的單位) 計算的單位可以是"H"(小時), "M"(分鐘), "S"(秒數),
或是"MS"(毫秒)
回傳數值是第一個時間減第二個時間差幾個小時，幾分鐘，幾秒鐘，或是幾毫秒

### 參數

-   **時間1**（type: 1）
-   **時間2**（type: 1）
-   **HMS時間基數**（type: 3）