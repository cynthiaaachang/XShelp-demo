## GetBarOffset（取得相對K棒位置）

**回傳類型**: 1\
**參數數量**: 1

### 說明

依傳入的交易日的日期取得相對K棒位置。 回傳數值=GetBarOffset(日期)
回傳數值=GetBarOffset(日期，時間) 傳入二個參數： -
第一個參數是交易日的日期，格式為YYYYMMDD。 -
第二個參數是時間，格式為HHMMSS，第二個參數可以不用傳。

### 參數

-   **target**（type: 1）