## Cover（把策略的空單部位變小，進行空單回補(減碼)的動作）

**回傳類型**:\
**參數數量**: 2

### 說明

把策略的空單部位變小，進行空單回補(減碼)的動作。 Cover(空單回補數量)
Cover(空單回補數量，委託價格) Cover(空單回補數量，label:="指令標記")
Cover(空單回補數量，委託價格, label:="指令標記")

### 參數

-   **數量**（type: 1）
-   **價格**（type: 1）